{
  "name": "@tokenring-ai/scraperapi",
  "version": "0.2.0",
  "description": "Advanced web scraping and SERP retrieval with proxy support.",
  "dependencies": {
    "@tokenring-ai/app": "workspace:*",
    "zod": "^4.3.6",
    "@tokenring-ai/websearch": "workspace:*",
    "@tokenring-ai/utility": "workspace:*"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "tsc --noEmit"
  },
  "type": "module",
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  }
}
